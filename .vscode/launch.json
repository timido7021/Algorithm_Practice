{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) 시작",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/a.out",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "My Task"
        },
    ]
}